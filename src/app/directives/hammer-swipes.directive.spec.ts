import { HammerSwipesDirective } from './hammer-swipes.directive';

describe('HammerSwipesDirective', () => {
  it('should create an instance', () => {
    const directive = new HammerSwipesDirective();
    expect(directive).toBeTruthy();
  });
});
